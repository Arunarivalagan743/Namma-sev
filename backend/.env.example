# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB
# For local development:
MONGODB_URI=mongodb://localhost:27017/namsev_db
# For MongoDB Atlas (production):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/namsev_db?retryWrites=true&w=majority

# Firebase Configuration
# Get these from: Firebase Console > Project Settings > Service Accounts > Generate New Private Key
#
# Option 1: Individual fields (RECOMMENDED)
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-Private-Key-Here\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com

# Option 2: Full JSON string (alternative)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}

# Admin Configuration
# The first user who registers with this email will be auto-assigned as admin
ADMIN_EMAIL=panchayat.office@gmail.com

# Google Translate API (Optional)
# Get from: Google Cloud Console > APIs & Services > Credentials
# Only needed if you want automatic translation features
GOOGLE_TRANSLATE_API_KEY=your-google-translate-api-key-here

# CORS Configuration
# Frontend URL for CORS (update for production)
FRONTEND_URL=http://localhost:5173

